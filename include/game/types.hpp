#ifndef TYPES_HPP
#define TYPES_HPP

namespace Game {

enum class Color {
    Green,
    Red,
    Yellow,
    Blue
};

enum class Turn {
    Computer,
    Player
};

//enum class GameState { };
//typedef enum { } PlayerState;

} // namespace Game

#endif // TYPES_HPP
